[project]
name = "montyhall"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
montyhall = "montyhall.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

#[tool.hatch.build.targets.wheel]
#packages = ["montyhall"]